{
  "name": "pasos-uniones-guia",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
